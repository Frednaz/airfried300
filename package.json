{
  "name": "airfried300",
  "version": "0.0.1",
  "private": true,
  "description": "Air-fryer recipe site — 300 recipes, one basket",
  "scripts": {
    "build": "node scripts/build-recipes.js",
    "dev": "node scripts/build-recipes.js && npx serve . -l 8000"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^16.0.0"
  }
}
